# investigate timeout issue - buffer yt stream in memory?
# download and seek stream to where failed when PCM channel closed
# read the stream at 2x speed to early detect failures and retry

# change the voice channel if the user is somewhere else
# Play and pause buttons
# put playlists in the correct order based on snippet.position
# Retrieve entirety of playlists